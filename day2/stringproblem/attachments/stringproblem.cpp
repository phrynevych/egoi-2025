#include <bits/stdc++.h>

using namespace std;

int main() {
    int N;
    cin >> N;

    vector<int> a(N);
    vector<int> b(N);
    for (int i = 0; i < N; ++i) {
        cin >> a[i] >> b[i];
    }

    int K = 0;

    cout << K << endl;

    for (int i = 0; i < K; ++i) {
        int p = 0;
        int s = 0;
        int e = 0;
        cout << p << ' ' << s << ' ' << e << endl;
    }

    return 0;
}
