#include <bits/stdc++.h>

using namespace std;

int main() {
    int T, N;
    cin >> T >> N;

    vector<int> a(N);
    for (int i = 0; i < N; ++i) {
        cin >> a[i];
    }

    int l = 0;
    int r = 0;

    cout << l << ' ' << r << endl;

    return 0;
}
